Hask table is also known as a hash map. It is a data structure that stores key-value pairs.
It uses a hask function to map keys to a fixed-size array, called a hash table. This allows
in faster search, insertion, and deletion operations.